const express = require("express");
const { Client, RichEmbed } = require("discord.js");
const app = express();
const chalk = require("chalk");
const fs = require("fs");
const ytdl = require("ytdl-core");
function sleep(milliseconds) {
  var start = new Date().getTime();
  for (var i = 0; i < 1e7; i++) {
    if (new Date().getTime() - start > milliseconds) {
      break;
    }
  }
}
const log = message => {
  console.log(`${message}`);
};

const Discord = require("discord.js");
const client = new Discord.Client();
const data = new Map();

const retnox2 = new Discord.Client();
const retnox3 = new Discord.Client();
const retnox4 = new Discord.Client();
const retnox5 = new Discord.Client();
const retnox6 = new Discord.Client();
const retnox7 = new Discord.Client();
const retnox8 = new Discord.Client();
const retnox9 = new Discord.Client();
const retnox10 = new Discord.Client();
const retnox11 = new Discord.Client();
const retnox12 = new Discord.Client();
const retnox13 = new Discord.Client();
const retnox14 = new Discord.Client();
const retnox15 = new Discord.Client();
const retnox16 = new Discord.Client();
const retnox17 = new Discord.Client();
const retnox18 = new Discord.Client();
const retnox19 = new Discord.Client();
const retnox20 = new Discord.Client();
const retnox21 = new Discord.Client();
const retnox22 = new Discord.Client();
const retnox23 = new Discord.Client();
const retnox24 = new Discord.Client();
const retnox25 = new Discord.Client();
const retnox26 = new Discord.Client();
const retnox27 = new Discord.Client();
const retnox28 = new Discord.Client();
const retnox29 = new Discord.Client();
const retnox30 = new Discord.Client();
const retnox31 = new Discord.Client();
const retnox32 = new Discord.Client();
const retnox33 = new Discord.Client();
const retnox34 = new Discord.Client();
const retnox35 = new Discord.Client();
const retnox36 = new Discord.Client();
const retnox37 = new Discord.Client();
const retnox38 = new Discord.Client();
const retnox39 = new Discord.Client();
const retnox40 = new Discord.Client();
const retnox41 = new Discord.Client();
const retnox42 = new Discord.Client();
const retnox43 = new Discord.Client();
const retnox44 = new Discord.Client();
const retnox45 = new Discord.Client();
const retnox46 = new Discord.Client();
const retnox47 = new Discord.Client();
const retnox48 = new Discord.Client();
const retnox49 = new Discord.Client();
const retnox50 = new Discord.Client();
const retnox51 = new Discord.Client();
const retnox52 = new Discord.Client();
const retnox53 = new Discord.Client();
const retnox54 = new Discord.Client();
const retnox55 = new Discord.Client();
const retnox56 = new Discord.Client();
const retnox57 = new Discord.Client();
const retnox58 = new Discord.Client();
const retnox59 = new Discord.Client();
const retnox60 = new Discord.Client();
const retnox61 = new Discord.Client();
const retnox62 = new Discord.Client();
const retnox63 = new Discord.Client();
const retnox64 = new Discord.Client();
const retnox65 = new Discord.Client();
const retnox66 = new Discord.Client();
const retnox67 = new Discord.Client();
const retnox68 = new Discord.Client();
const retnox69 = new Discord.Client();
const retnox70 = new Discord.Client();
const retnox71 = new Discord.Client();
const retnox72 = new Discord.Client();
const retnox73 = new Discord.Client();
const retnox74 = new Discord.Client();
const retnox75 = new Discord.Client();
const retnox76 = new Discord.Client();

const retnox1 = new Discord.Client()
const Constants = require('discord.js/src/util/Constants.js')
Constants.DefaultOptions.ws.properties.$browser = `Discord iOS`


retnox2.on("ready", () => {
retnox2.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox2.on("ready", () => {
retnox2.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox3.on("ready", () => {
retnox3.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox4.on("ready", () => {
retnox4.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox5.on("ready", () => {
retnox5.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox6.on("ready", () => {
retnox6.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox7.on("ready", () => {
retnox7.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox8.on("ready", () => {
retnox8.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox9.on("ready", () => {
retnox9.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox10.on("ready", () => {
retnox10.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox11.on("ready", () => {
retnox11.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox12.on("ready", () => {
retnox12.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox13.on("ready", () => {
retnox13.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox14.on("ready", () => {
retnox14.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox15.on("ready", () => {
retnox15.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox16.on("ready", () => {
retnox16.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox17.on("ready", () => {
retnox17.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox18.on("ready", () => {
retnox18.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox19.on("ready", () => {
retnox19.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox20.on("ready", () => {
retnox20.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox21.on("ready", () => {
retnox21.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox22.on("ready", () => {
retnox22.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox23.on("ready", () => {
retnox23.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox24.on("ready", () => {
retnox24.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox25.on("ready", () => {
retnox25.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox26.on("ready", () => {
retnox26.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox27.on("ready", () => {
retnox27.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox28.on("ready", () => {
retnox28.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox29.on("ready", () => {
retnox29.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox30.on("ready", () => {
retnox30.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox31.on("ready", () => {
retnox31.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox32.on("ready", () => {
retnox32.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox33.on("ready", () => {
retnox33.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox34.on("ready", () => {
retnox34.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox35.on("ready", () => {
retnox35.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox36.on("ready", () => {
retnox36.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox37.on("ready", () => {
retnox37.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox38.on("ready", () => {
retnox38.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox39.on("ready", () => {
retnox39.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox40.on("ready", () => {
retnox40.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox41.on("ready", () => {
retnox41.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox42.on("ready", () => {
retnox42.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});

retnox43.on("ready", () => {
retnox43.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});

retnox44.on("ready", () => {
retnox44.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});

retnox45.on("ready", () => {
retnox45.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});

retnox46.on("ready", () => {
retnox46.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});

retnox47.on("ready", () => {
retnox47.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});

retnox48.on("ready", () => {
retnox48.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});

retnox49.on("ready", () => {
retnox49.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});

retnox50.on("ready", () => {
retnox50.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});

retnox51.on("ready", () => {
retnox51.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox52.on("ready", () => {
retnox52.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox53.on("ready", () => {
retnox53.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox54.on("ready", () => {
retnox54.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox55.on("ready", () => {
retnox55.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox56.on("ready", () => {
retnox56.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox57.on("ready", () => {
retnox57.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox58.on("ready", () => {
retnox58.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox59.on("ready", () => {
retnox59.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox60.on("ready", () => {
retnox60.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox61.on("ready", () => {
retnox61.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox62.on("ready", () => {
retnox62.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox63.on("ready", () => {
retnox63.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox64.on("ready", () => {
retnox64.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox65.on("ready", () => {
retnox65.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox66.on("ready", () => {
retnox66.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox67.on("ready", () => {
retnox67.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox68.on("ready", () => {
retnox68.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox69.on("ready", () => {
retnox69.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox70.on("ready", () => {
retnox70.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox71.on("ready", () => {
retnox71.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox72.on("ready", () => {
retnox72.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox73.on("ready", () => {
retnox73.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox74.on("ready", () => {
retnox74.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox75.on("ready", () => {
retnox75.user.setActivity("🎈 Varus Bot", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});
retnox76.on("ready", () => {
retnox76.user.setActivity("🎈 Sıkılıyorum", { type: "STREAMING", url: "https://www.twitch.tv/wraihgts" })
});



retnox1.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1 };
    var kanal = msg.member.voiceChannel;
    kanal.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      })
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox2.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox3.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox4.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox5.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("s", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox6.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox7.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox8.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox9.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox10.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox11.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox12.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox13.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox14.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox15.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox16.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox17.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox18.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox19.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox20.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox21.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox22.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox23.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox24.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox25.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox26.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox27.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox28.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox29.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox30.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox31.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox32.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox33.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox34.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox35.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox36.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox37.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox38.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox39.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox40.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox41.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox42.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox43.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox44.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox45.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox46.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox47.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox48.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox49.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox50.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox51.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox52.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox53.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox54.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox55.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox56.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox57.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox58.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox59.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox60.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox61.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox62.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox63.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox64.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox65.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox66.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox67.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox68.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox69.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox70.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox71.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox72.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox73.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox74.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

retnox75.on("message", async msg => {
  if (msg.content.toLowerCase() === ".ses") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
}); 

retnox1.on('ready', message => {
	console.log(`${retnox1.user.username} ismi ile giriş yapıldı!`);
});

retnox1.login("NzI3ODU5NjAzMDEzODk0MjA1.Xvx_Tw.D6H9jEYlGVygZPbjje1o0dYEnEA");
retnox2.login("NzI3ODYxMDkxNTA5NzMxNDI4.XvyA6A.HmokZzk4DM7DQGkobAzLpi-JagM");
retnox3.login("NzI3ODYyNzc5NDU2NzgyMzQ2.XvyDHw.m0WnSWxjiUEDs2su9-DbHMnw7A8");
retnox4.login("NzI3ODY1NDExOTY5ODc2MDA4.XvyDxQ.KCioHtmARBEV2GvQV8V8S1fbKPA")
retnox5.login("NzI3ODY1OTA3MDk4OTQzNjAx.XvyESA.YDjlvZOCBz__tsP49uUPzqjaU3g");
retnox6.login("NzI3ODY2NDI1NDI2ODM3NTY2.XvyEyw.BV-uRfYlgMilmOVwaml7Rfx_JX0");
retnox7.login("NzI3ODY3MTE2ODc0NjI5MTcy.XvyFmQ.Zz8o1hyRubH6PZVzQl2Y0yN_AI4");
retnox8.login("NzI3ODY4MzA3OTc3MjczMzQ2.XvyGfg.Hfz8eN3sIcsmXhi09gAcbhuxUh4");
retnox9.login("NzI3ODY4Nzg2NjM5ODk2NTc4.XvyHCQ.J-LktNw2A5cTo5DL3rdsCjwNonc");
retnox10.login("NzI3ODY5NDU0MTUwNzk1MzY2.XvyIAg.x63fQ_PUxJKL6MsnFm5SrIVjAoY");
retnox11.login("NzI3ODcwNDQ0ODQ5NTk0NDU5.XvyIeg.CzfQb_9jiCgb0MhN2M3ZZ0kl1GA");
retnox12.login("NzI3ODcxMDk0ODU3NjYyNTU3.XvyI-g.t-q1ZtAxZphmLn0hQw06-4Nt6vA");
retnox13.login("NzI3ODcxNDQ2MzkwNjY5Mzgz.XvyJdQ.FDEsE_1kZAWt4M2tY0ABbxO3UgM");
retnox14.login("NzI3ODcyMDE1NTk5NTM0MDkw.XvyKGQ.8n9Rti-nTSyXsGQ6T4XQnvLZzL0");
retnox15.login("NzI3ODcyOTA4OTUyODYyNzYx.XvyLDA.dSz9zogMpB6X7a6stiWJyydx48k");
retnox16.login("NzI3ODczNzkwNDk5MDk0NTM4.XvyLnA.RyK1xMB6gSsPgtN5XcVYpWtrJKI");
retnox17.login("NzI3ODc0MjkzMzY2Nzg0MDgy.XvyMJQ.4wQBhfZcC_i5ZASJ1SFpfDczHUE");
retnox18.login("NzI3ODc1MDIyMjY5NTc5MjY1.XvyMnw.UFfwZu8fpH4N0nEcmSMzd8P7P10");
retnox19.login("NzI3ODc1Mzg0ODE2OTU5NTE4.XvyNGg.FtorRDVsaOKb3VB8V4iZIjgIQzA");
retnox20.login("NzI3ODc2MzQ4NTc1NjEzMDM5.XvyNyA.k49KwQ0m_3vPR2Bs9hq-o4xjg3A");
retnox21.login("NzI3ODc2NjU1NjQ0NjcyMTUy.XvyOVQ.3ludpxA24QYq7gGoHR5Ds_gT62M");
retnox22.login("NzI3ODc3MjAyMDAzMzYxODMy.XvyO0g.5y8_dS28FxxKsDrc8RtNbBgO59k");
retnox23.login("NzI3ODc3NjU4OTUxNjgwMDIy.XvyPWA.cyNv5-Sdq41EMDHk2V548fOzOaY");
retnox24.login("NzI3ODc4MjM0MzEzNzE5ODc5.XvyP2w.Wg4Av5mXWxVks7rzYtvfw3_nxfg");
retnox25.login("NzI3ODc5MzQwMTc1NTIzODgy.XvyQ4A.RvLBn2UUMxMw2a7BerctGvJ9pmc");
retnox26.login("NzI3ODc4NzcyMTc4NjgxODk2.XvyQXA.IJXeCvCj0hHHXnQ0nROagEbuSxU");
retnox27.login("NzE0ODMzNzUxMDM2NjU3NzE0.Xs0bOQ.0y1o5gTGzwx1yxum1DPoGm53lao");
retnox28.login("NzE0ODM0NzQ1NzM2ODIyODQ2.Xs0cDw.PTPGQZgXAlPG9Tu-ISomrAyOi-s");
retnox29.login("NzE0ODE4NzUzMDIzNDQyOTU1.Xs0XNA.Eq5EFQ-MVN_co6Qv5kMouehe79g");
retnox30.login("NzE0ODU1Mzc1MjE1NTI2MDIw.Xs0vzg.icrRItsXxgkFkeHwdod0evaSpvE");
retnox21.login("NzE0ODU2OTE4NzkwOTYzMjQy.Xs0wcQ.LVo0sShv0tJTwW0cOP0FDqoYyYk");
retnox32.login("NzE0ODU3MzA4NjcyMzYwNTM5.Xs0w7w.41mGg_8lEqelN6Xro6p3IzK0z_c");
retnox33.login("NzE0ODU3NzcxMDQzOTEzNzM3.Xs0xcQ.YZERFphOddmkVx2RGN1CetJNp-4");
retnox34.login("NzE0ODU4MjY3MTk2Nzg0NjQy.Xs0yHw.HWEqfi7OSHfDJgaFRLMnprw1f4U");
retnox35.login("NzE0ODU4OTAzMzI1OTAwODY0.Xs0yrw.gKgzD7_ErCjnrEXSy8Y7w2rkOLI");
retnox36.login("NzE0ODYwNTEyMzc0MDMwMzU2.Xs0zvQ.dYPn9b8WdRp_R7gkKS3qVKpFzaY");
retnox37.login("NzE0ODYwNTEyMzc0MDMwMzU2.Xs0zvQ.dYPn9b8WdRp_R7gkKS3qVKpFzaY");
retnox38.login("NzE0ODYxMjM2MzY1NTU3OTMz.Xs008g.yPjK6wB2h9wcPN318LipNVBSpLg");
retnox39.login("NzE0ODYyMDE1NjIwODQxNTIy.Xs01gg.dbbPckyVDEuNzl_VbeIKGWdes5o");
retnox40.login("NzE0ODYzNjgwMDg0MjQ2NTI4.Xs02yw.jhMTUU3w6CsdUSQ4n29PiYdU_o4");
retnox41.login("NzE0ODY0MDA5NjUxOTQ1NTAy.Xs03uw.8Nais_9LAtaUyyH2QV0MlScgCC4");
retnox42.login("NzE0ODY1MTc1NTUxMDE2OTgw.Xs04VQ.WCtYPBoOnpu76dYFey-3VW1xOZ0");
retnox43.login("NzE0ODY1NTQ4MDU5NzM4MjEz.Xs04zg.DZWD6b-VkvDT9Vf7TrbMXr_xgyo");
retnox44.login("NzE0ODY2MTM4NjYzODc4NzI2.Xs05TQ.jhLWoRUGE3UFZiqKxAV84RtYCGg");
retnox45.login("NzE0ODY2NTkzODc5ODE4MzYy.Xs05xg.sx_WtbXba5LWRRAxZeddkvaN1UM");
retnox46.login("NzE0ODY3MjUyMDM3NTUwMTAw.Xs06Yg.Tv71mgBfhqHN8tWfYeKU9WGpjWg");
retnox47.login("NzE0ODY3ODM4MTc4OTUxMjc5.Xs067Q.HbWPweRr9R5A3DSNtwd2lxTq8rk");
retnox48.login("NzE0ODY4NDI0MTM1NjcxODY5.Xs09tw.XwDi8_VDOvtgY92SB8UPV8uaQYg");
retnox49.login("NzE0ODcyMjE5NTExMjkxOTA1.Xs0-sQ.prykBEtPz8vJxDz4k4SuFdDaiiM");
retnox50.login("NzE5OTEyODQ1MzY5OTMzODcw.Xt-Vaw.2sKainCMZmqJszSiIWkI29KDrwc");
retnox51.login("NzE5OTEzNTEwOTAwODU4ODkw.Xt-WNA.5X8y4dgLvUZiVERGGJQz8AhWFnM");
retnox52.login("NzE5OTE2MjM4NzMzNTc0MjI2.Xt-Ykg.KKxsxdbaem85ZDJndLUz-n7Eo0U");
retnox53.login("NzE5OTE0NDU4MDQxODExMDE1.Xt-WxQ.PWvg1QMzTWrf5U3nhS292GTFMpw");
retnox54.login("NzE5OTE2MjM4NzMzNTc0MjI2.Xt-Ykg.KKxsxdbaem85ZDJndLUz-n7Eo0U");
retnox55.login("NzE5OTE3MjkyNzg3NTk3Mzg0.Xt-Zrw.radHGQ74d7h_70bJjz5cfYFFexk");
retnox56.login("NzE5OTE3OTY0MzkxODc0NTgw.Xt-aLA.vPguzYNBeMn1Ue9Fo2Y6OsZrup0");
retnox57.login("NzE5OTMyMDU0MjQxOTM1NDAx.Xt-nJA.D_FyJ-uKtcY3fHm3blHJ-s45OJI");
retnox58.login("NzE5OTMzMDc5MzA0NTM2MTAw.Xt-okA.8c82jwu88OGmwe35ZfwYgrevp5g");
retnox59.login("NzE5OTM0MDEwOTk3OTk3Njc5.Xt-pIw.5WxhWMrcFJILBOtWkZnI7wEFwAU");
retnox60.login("NzE5OTM0NTQzNTUzMTAxOTQ1.Xt-ppg.5yfa1AAhx-xgStHw1hZL3U8xtlE");
retnox61.login("NzE5OTM1MjExMzUzNDA3NTU5.Xt-qKQ.a9gtk3UtYA1sZdOAbFE7rhigBhY");
retnox62.login("NzE5OTM1NzUxMTIyNDUyNDgw.Xt-qvw.dJ7_PEFZYQL3uhgOq5VO3jc1VOg");
retnox63.login("NzE4NTA1MTgyMzk2MTUzODU2.Xtp2QA.MqdYOEZzO5aPRCP7TwvB_JMEFuI");
retnox64.login("NzE4NTA1OTYyNDczOTE0NDI4.Xtp3Dw.8P3Be_THSBQ-ae9UgLjK69Ki6KE");
retnox65.login("NzE4NTA2NzIzNDQyNDI1OTA3.Xtp3rA.tlHUkXOnzRGwgEhAYC46TZAY6Ng");
retnox66.login("NzE4NTA4MjA5MDU2MzgzMDM4.Xtp4_A.lWh3n39VqJZjA8TS1H89tjWU8y8");
retnox67.login("NzE4NTA5NjE1MzE4NDMzODIy.Xtp6iQ.j_s1j15ASCHAaJ7hFG1UD29PvlE");
retnox68.login("NzE4NTEwNDgyNTExNzU3NDI0.Xtp7Jw.zzFux1I6KyAaQmoDzNtvZFBdUpY");
retnox69.login("NzE4NTExMTMzNDM4NTA5MTU5.Xtp7sw.8kLLcbW9jAZtv6A_b2FXxFIPE78");
retnox70.login("NzE4NTExNzAxNTUyNjYwNDgw.Xtp8PA.ua19CUbSzxLGGE6-mm4R5vZxAK0");
retnox71.login("NzE4NTEyMjQ1NjM3NjQ0Mzc4.Xtp9Bg.41_OVt_1DLURtGN0oYSukvOkgxY");
retnox72.login("NzE4NTEzMDkwMzk1NzY2ODY0.Xtp9mg.g-tDoDb32Go4JPt7slNl8VlH2x4");
retnox73.login("NzE4NTA1MTgyMzk2MTUzODU2.Xtp2QA.MqdYOEZzO5aPRCP7TwvB_JMEFuI");
retnox74.login("NzE4NTA1OTYyNDczOTE0NDI4.Xtp3Dw.8P3Be_THSBQ-ae9UgLjK69Ki6KE");
retnox75.login("NzE4NTA2NzIzNDQyNDI1OTA3.Xtp3rA.tlHUkXOnzRGwgEhAYC46TZAY6Ng");
retnox76.login("NDY3NjY4NTkxNzMxNTM5OTcw.XtglfA.4knT742B7FO3EV8ANY0mImhmDLs");//NEQZY

//Yine Bir Neqzy Harikası